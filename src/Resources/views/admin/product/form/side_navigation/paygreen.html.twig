{% if hookable_metadata.context.form.variant is defined and hookable_metadata.context.form.variant.mealVoucherCompatible is defined %}
    <button
        type="button"
        class="list-group-item list-group-item-action {% if hookable_metadata.configuration.active|default(false) %}active{% endif %}"
        data-bs-toggle="tab"
        data-bs-target="#product-paygreen"
        role="tab"
    >
        PayGreen
    </button>
{% endif %}
